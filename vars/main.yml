---
# vars file for lighttpd

lighttpd_user: "{{ lighttpd_os_users[ansible_os_family] | default('www-data') }}"

lighttpd_group: "{{ lighttpd_os_groups[ansible_os_family] | default('www-data') }}"

# lighttpd_modules:
#   - status
#   - userdir

# lighttpd_config:
#   server.port: 8080
